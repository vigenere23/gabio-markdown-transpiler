{
  "name": "@gabio/markdown-transpiler-example",
  "version": "1.0.0",
  "description": "An example script for using @gabio/markdown-transpiler",
  "scripts": {
    "build": "yarn build:markdown && yarn format",
    "build:markdown": "ts-node -r tsconfig-paths/register main.ts",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "prettier": "^2.1.2",
    "prettier-plugin-svelte": "^1.4.1",
    "svelte": "^3.29.4",
    "ts-node": "^9.0.0",
    "tsconfig-paths": "^3.9.0",
    "tslint-config-prettier": "^1.18.0"
  },
  "dependencies": {}
}
